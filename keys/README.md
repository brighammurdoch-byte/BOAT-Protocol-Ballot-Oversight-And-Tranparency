# Program keypair

`boat_final-keypair.json` defines program id
`DgVtAKNDKiTYUowPBsXfDnv7Seq5hE3NsP1oMDexCoid`.

Copy to `target/deploy/boat_final-keypair.json` before `anchor build` / deploy so the
id stays stable. Upgrade authority is the wallet that deploys, not this file alone.
